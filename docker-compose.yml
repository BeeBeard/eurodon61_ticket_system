services:

  ticket-system:

    build: .

    container_name: ticket-system

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: unless-stopped